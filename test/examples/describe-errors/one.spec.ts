fdescribe('My Fancy Thing', () => {
  // should ignore fdescribe or xdescribe

  beforeEach(() => {
    // Implementation
  });

  it('should instantiate', () => {
    // Implementation
  });

  it('should compare true with true', () => {
    // should ignore fit or xit
    expect(true).toBe(true);
  });
});
